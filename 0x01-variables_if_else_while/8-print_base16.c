#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
char c;
for ( c = 0; c <= 15; ++c)
hexa = c / 16;
putchar('0' + hexa);
putchar ('\n');  
return (0);
}
