#include <stdio.h>

/**
 * main - Prints the number of arguments passed into it
 * @argc: the counter of arguments
 * @argv: array of strings that take the strings of the arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{

printf("%d\n", argc);
(void) argv;
return (0);
}
