#include <stdio.h>
/**
 * main - print the name of the program
 * @argc: the size of the command line
 * @argv: a pointer of array that contain the arguments of commands line
 * Return: 0
 */

int main(int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
