#include "main.h"

/**
 * int _abs - check the value of an integer
 * print the absolute value of that integer
 */

int _abs(int)
{
int n;
int result;
 
if (n < 0)
{
result = n * (-1);
putchar(result);
else
putchar(n);
}
}
